/** @type { import('@storybook/svelte').Preview } */
const preview = {
  parameters: {
    controls: {
      disable: true
    },
    actions: {
      disable: true
    }
  }
};

export default preview;
